/**
 * Hanzo World product IDs for frontend checkout CTAs.
 * Source: src/config/products.generated.ts (maps to plans/subscription.json).
 */

export {
  WORLD_PRODUCTS,
  DODO_PRODUCTS, // back-compat alias, same values
  DEFAULT_UPGRADE_PRODUCT,
} from './products.generated';
